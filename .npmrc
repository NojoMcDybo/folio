; Auf diesem Rechner sind NODE_ENV=production und omit=dev global gesetzt.
; Das laesst npm die devDependencies (vite, typescript) stillschweigend weg.
; Hier explizit wieder einschalten, damit das Projekt ueberall gleich baut.
include=dev
